meta {
  name: validate token
  type: http
  seq: 3
}

post {
  url: {{HOST}}auth/jwt/verify/
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  token: {{TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
